/**
 * Motion (Framer Motion) — tek import noktası.
 * @see https://motion.dev/docs/react-quick-start
 */
export {
  motion,
  AnimatePresence,
  MotionConfig,
  useReducedMotion,
  useInView,
  useScroll,
  useTransform,
} from 'motion/react'

export type { HTMLMotionProps, Transition, Variants } from 'motion/react'
